name: metal_shader_fx_example
description: Demonstrates how to use the metal_shader_fx package.
publish_to: "none"

environment:
  sdk: ^3.11.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  metal_shader_fx:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
